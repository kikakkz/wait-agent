kind: state
version: 1
current_task: task.github-issue-15-remote-output-routing
focus_statement: >
  active task is GitHub issue #15: fix remote output routed into wrong pane when multiple sessions share a host. The root cause is that remote main-slot output routing is validated at authority-node level but not at session/target/attachment level before forwarding output to the pane. Need to inspect apply_authority_envelope and related pane/runtime code, add target/session/attachment validation, and ensure output mismatches are dropped instead of mixed into the wrong pane.
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: task.github-issue-15-remote-output-routing.in_progress

kind: state
version: 1
current_task: task.cloud-auth-impl-keyring-secret-store
focus_statement: >
  Implementation of the cloud-host auth design has started. Current focus is
  task.cloud-auth-impl-keyring-secret-store: unify operator private key, SSH
  password, and sudo password in the OS keyring, implement KeyringSecretStore,
  and migrate existing encrypted file secrets on first run. No file fallback is
  allowed. After this task, the queue is: certificate rotation, startup reconnect
  expiry fallback, operator key rotation, and LAN host acceptance.
last_updated_from:
  file: .agents/tasks/backlog.yaml
  reference: task.cloud-auth-impl-keyring-secret-store

kind: state
version: 1
current_task: task.github-issue-16-hidden-windows-accumulate
focus_statement: >
  active task is GitHub issue #16: wa-hidden windows accumulate after remote abnormal disconnect/reconnect. The root cause is that remote panes are created with remain-on-exit=on, and abnormal exit paths (protocol error, pane-died, recovery) do not consistently call cleanup_exited_remote_session_pane. Need to analyze the exact code paths in main_slot_runtime.rs and remote_main_slot_pane_runtime.rs before proposing a fix.
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: task.github-issue-16-hidden-windows-accumulate.in_progress

kind: state
version: 1
current_task: null
focus_statement: >
  remote exit latency and transport stability batch is closed, including the follow-up fix that keeps remote main-slot panes alive when authority/control-plane registration is temporarily absent by deferring pane-owned resize and input state. Current focus is selecting the next remote-network completion task or deciding whether to un-defer workspace snapshot persistence.
last_updated_from:
  file: .agents/state/task-history.yaml
  reference: task.remote-main-slot-authority-gap-followup.done

kind: state
version: 1
current_task: task.tmux-r5
focus_statement: the active task is now stabilizing tmux-owned persistent chrome with native client-attached and client-resized reconcile hooks plus a single-row footer baseline while keeping the shared managed-session catalog usable for later network-backed session management
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: current_task

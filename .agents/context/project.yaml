id: context.project
kind: context
version: 1
summary:
  product: WaitAgent
  repo_type: single_crate_rust_repo
  language: rust
  package_manager: cargo
  architecture_style: tmux_first_local_workspace_runtime
  active_business_goal: resume mirrored network interaction on top of the accepted tmux-first local workspace baseline
  framework_policy: prefer mature maintained Rust components over custom infrastructure, and avoid weakly maintained dependencies across the codebase
current_phase:
  phase_id: phase-2
  phase_title: network aggregation MVP
  stage_id: network-registry-reset
  stage_title: resume remote design from the accepted tmux-first local baseline
current_focus:
  default_task_id: task.t5-06
  focus_statement: build the shared transport-agnostic target registry that remote interaction will consume, without regressing the accepted local fixed-chrome baseline
upcoming_focus:
  - route remote input and resize through the server control plane on top of the shared target catalog
  - introduce the server-side workspace console as a target-activation surface
  - keep local and remote target identity transport-agnostic instead of reviving local-only PTY assumptions
source_of_truth:
  - docs/execution-status-board.md
  - docs/local-acceptance-checklist.md
  - docs/architecture.md
  - docs/tmux-first-workspace-plan.md
  - docs/tmux-first-runtime-architecture.md
  - docs/functional-design.md

id: task.sidebar-1
kind: task
version: 1
title: finalize the v1 right-side session sidebar contract and split the rollout into implementation slices
status: done
priority: medium
source_of_truth:
  file: .agents/tasks/backlog.yaml
  reference: next
depends_on:
  - task.lifecycle-5.done
linked_primitives:
  - primitive.verification-refresh
  - primitive.task-history-update
  - primitive.task-board-sync
  - primitive.agents-self-maintenance
done_criteria:
  - the sidebar v1 navigation contract is fixed to unified WaitAgent-owned arrow-key navigation with `Right` entering the sidebar focus and `Left` returning to the main view
  - the sidebar v1 list contract is fixed to one row per session, using the detected agent name for agent-backed sessions and `bash` for ordinary shell sessions
  - the sidebar v1 state contract is fixed to `INPUT`, `CONFIRM`, and `UNKNOWN` wait-state badges without inventing extra automation behavior in this slice
  - the sidebar v1 detail contract is fixed to a bottom path detail area rather than a floating tooltip
  - the sidebar rollout is split into bounded implementation tasks so the future work does not depend on this placeholder file alone
verification:
  - user-approved design review defining unified navigation, row content, wait-state badges, bottom path detail, and the initial sidebar rollout split
touched_paths:
  - .agents/tasks/backlog.yaml
  - .agents/tasks/queued/task.sidebar-1.yaml
  - .agents/tasks/queued/task.sidebar-2.yaml
  - .agents/tasks/queued/task.sidebar-3.yaml
  - .agents/tasks/queued/task.sidebar-4.yaml
  - .agents/state/current-focus.yaml
  - docs/execution-status-board.md
notes:
  - Future automation rules such as commit-policy handling or auto-confirm rules are intentionally out of scope for this first sidebar rollout.
  - The approved v1 keeps the current rendering architecture in place and only narrows the sidebar interaction contract.

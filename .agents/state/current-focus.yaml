kind: state
version: 1
current_task: task.t6-01b
focus_statement: `task.t6-01a` is now closed; the current step is to capture real `submit` and manual-switch signals through the new unified server-console interaction seam before queue-order or auto-switch work
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: current_task

kind: state
version: 1
current_task: task.event-r3
focus_statement: task.event-r2a umbrella acceptance is now closed, so the active gate moves to task.event-r3: remove remaining accepted-path attach, resize, and scheduler tick or timeout coordination in favor of explicit runtime events
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: current_task

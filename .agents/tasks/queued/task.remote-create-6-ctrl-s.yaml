id: task.remote-create-6-ctrl-s
kind: task
version: 1
title: wire Ctrl-S to create a session on the selected connected remote endpoint
status: pending
priority: high
design_ref:
  doc: docs/remote-host-connect-and-session-creation-design.md
  why:
    - Accepted design v1.1 defines this bounded implementation slice.
source_of_truth:
  file: .agents/tasks/backlog.yaml
  reference: next
depends_on:
  - task.remote-create-2-sidebar-selection-state.done
  - task.remote-create-5-server-service.done

linked_primitives:
  - primitive.verification-refresh
  - primitive.task-board-sync
done_criteria:
  - Ctrl-S resolves the selected remote endpoint, rejects invalid selections clearly, creates a new remote session, waits for sync, and activates it.
verification:
  - cargo test remote_create ctrl_s -- --test-threads=1
  - cargo build

touched_paths:
  - src/cli.rs
  - src/command/dispatch.rs
  - src/runtime/workspace/workspace_command_runtime.rs
  - src/runtime/workspace/main_slot_runtime.rs

notes:
  - Keep changes scoped to this slice; do not implement later Ctrl-W/Ctrl-S behavior early unless required by a dependency boundary.
  - Preserve PTY ownership and runtime-owner catalog boundaries from the accepted design.

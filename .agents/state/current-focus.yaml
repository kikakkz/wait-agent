kind: state
version: 1
current_task: task.event-r3
focus_statement: task.event-r3 is now centered on removing tick-driven attach and scheduler control, because the pane-side chrome path is already event-driven and the next accepted slice is the remaining local control loop in `src/app.rs` and related attach runtimes
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: current_task

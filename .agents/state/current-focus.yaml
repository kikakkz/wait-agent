kind: state
version: 1
current_task: task.ratatui-phase-3-remote-reconnect
focus_statement: >
  Starting ratatui migration completion queue: implement automatic reconnect for
  ratatui remote sessions. Currently a remote session disappears from the local
  sidebar as soon as the authority transport drops; the session should survive the
  disconnect, reconnect when the remote node comes back, and resume output without
  requiring the user to Ctrl-W again. Next step is plan-mode design.
last_updated_from:
  file: .agents/tasks/backlog.yaml
  reference: task.ratatui-phase-3-remote-reconnect.in_progress

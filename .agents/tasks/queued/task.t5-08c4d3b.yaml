id: task.t5-08c4d3b
kind: task
version: 1
title: implement explicit remote mirror open or close protocol messages and server-side session-route ownership
status: in_progress
priority: high
design_ref:
  doc: docs/remote-live-mirror-design.md
  why:
    - the current remote session surface can open a row but cannot request a real live mirror for that session
    - this slice establishes the server-owned per-session mirror route plus explicit protocol messages instead of implicit placeholder-only behavior
source_of_truth:
  file: .agents/tasks/current.yaml
  reference: current
depends_on:
  - task.t5-08c4d3a.done
linked_primitives:
  - primitive.verification-refresh
  - primitive.task-history-update
  - primitive.task-board-sync
  - primitive.agents-self-maintenance
done_criteria:
  - the protocol and transport facade expose explicit mirror open or close messages for one remote `session_id`
  - server-side remote open attaches observers to one per-session mirror route instead of relying on placeholder state alone
  - opening the same remote session from more than one console reuses the same server-side mirror route
verification:
  - cargo test
  - targeted review of protocol and server mirror-route ownership
touched_paths:
  - proto/waitagent/remote/v1/node_session.proto
  - src/infra/remote_grpc_proto.rs
  - src/infra/remote_grpc_transport.rs
  - src/infra/remote_protocol.rs
  - src/runtime/remote_node_ingress_runtime.rs
  - src/runtime/remote_node_session_runtime.rs
  - src/runtime/remote_main_slot_ingress_runtime.rs
  - src/runtime/remote_main_slot_runtime.rs
  - src/runtime/remote_main_slot_pane_runtime.rs
  - docs/protocol.md
  - docs/execution-status-board.md
  - .agents/tasks/backlog.yaml
  - .agents/tasks/current.yaml
  - .agents/tasks/queued/task.t5-08c4d3b.yaml
  - .agents/state/current-focus.yaml
  - .agents/state/task-history.yaml
notes:
  - This slice owns the protocol and server route boundary only. It should not yet finish PTY-owner mirror reuse or final acceptance.


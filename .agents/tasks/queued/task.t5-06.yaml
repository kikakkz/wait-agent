id: task.t5-06
kind: task
version: 1
title: implement the aggregate server session registry
status: deferred
priority: high
target_task_reference: task.t5-06
source_of_truth:
  file: .agents/tasks/backlog.yaml
  reference: active
depends_on:
  - task.lifecycle-5.done
linked_primitives:
  - primitive.network-registry-scope
  - primitive.verification-refresh
  - primitive.task-history-update
  - primitive.task-board-sync
  - primitive.agents-self-maintenance
done_criteria:
  - the aggregate server tracks registered local and remote sessions in one authoritative registry instead of relying on one-off publication paths
  - registry state is structured so later remote resize, input routing, and server-side console work can consume it without another schema rewrite
  - local-only workspace behavior remains intact while aggregate registration is introduced
  - task board and verification state are refreshed together with the implementation slice
verification:
  - cargo test
  - targeted review of registry ownership and update paths across the server, transport, and session layers
touched_paths:
  - src/server.rs
  - src/session.rs
  - src/transport.rs
  - src/app.rs
  - .agents/tasks/current.yaml
  - .agents/tasks/backlog.yaml
  - .agents/tasks/queued/task.t5-06.yaml
  - .agents/state/current-focus.yaml
notes:
  - This task is deferred behind the tmux-first local migration because continuing network expansion on the retired custom display baseline would multiply later debugging cost.

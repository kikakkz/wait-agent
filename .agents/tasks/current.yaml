id: task.t4-10
kind: task
version: 1
title: finish local acceptance sign-off for the single-entry workspace UX
status: in_progress
priority: highest
source_of_truth:
  file: docs/execution-status-board.md
  reference: Current Focus And Next Queue
depends_on:
  - task.t4-09.done
linked_primitives:
  - primitive.acceptance-check-sync
  - primitive.local-workspace-validation
  - primitive.verification-refresh
  - primitive.blocker-record
  - primitive.task-history-update
  - primitive.task-board-sync
  - primitive.agents-self-maintenance
linked_runbooks:
  - runbook.continue-default-work
  - runbook.close-local-acceptance
done_criteria:
  - the local acceptance checklist is rerun in a real daily-use terminal environment
  - the validated agent mix and any remaining local UX issues are explicitly recorded
  - any issue serious enough to multiply network debugging cost is either fixed or recorded as a blocker
  - .agents state and docs/execution-status-board.md agree on whether T4-10 remains open or can close
verification:
  - cargo test
  - manual: rerun docs/local-acceptance-checklist.md sections 5.1 through 5.8
  - manual: confirm docs/local-acceptance-checklist.md section 6 acceptance rules
touched_paths:
  - docs/execution-status-board.md
  - docs/local-acceptance-checklist.md
  - src/app.rs
  - src/terminal.rs
  - .agents/
  - docs/agents-control-plane.md
blockers:
  - final sign-off still depends on user-terminal validation with the preferred real agent mix
notes:
  - do not reopen broad network work until the local acceptance gate is explicitly closed
  - the current worktree already contains ongoing edits related to acceptance and terminal behavior
  - 2026-04-15 manual PTY validation reconfirmed multi-session lifecycle, shell cwd preservation, UTF-8 Chinese echo, picker behavior, Peek input ownership, and fullscreen shell return to prompt
  - 2026-04-15 managed Codex validation confirmed startup UI rendering, prompt visibility, and live-surface takeover inside waitagent with a background shell session still present
  - 2026-04-15 src/app.rs was updated so Ctrl-C is no longer parsed as QuitHost while the focused session owns live passthrough display; cargo test covers the parser gate
  - 2026-04-15 a human-style repro path for the switch-back display bug was established as waitagent -> Ctrl-N -> codex -> Ctrl-B -> Ctrl-F
  - 2026-04-15 src/app.rs was further updated so live-surface chrome only returns after reactivation if post-reactivation output arrives; old pre-switch output no longer qualifies
  - PTY-tool validation of raw Ctrl-C passthrough remains inconclusive because the outer test harness converts Ctrl-C into an interrupt for the whole PTY process
  - managed Codex validation did not yet establish full end-to-end menu navigation and post-submit interaction completion in the preferred user terminal
  - final manual sign-off still requires validation in the user's preferred real terminal with the preferred agent mix, especially Codex-like TUI behavior

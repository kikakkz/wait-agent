kind: state
version: 1
current_task: task.remote-create-2-sidebar-selection-state
focus_statement: >
  task.remote-create-1-ui-keys is complete: Ctrl-N remains local-only, Prefix-c
  remains a hidden local alias outside primary UI copy, and Ctrl-W/Ctrl-S hidden
  entrypoints plus bindings are present with explicit first-slice placeholders.
  Current focus is task.remote-create-2-sidebar-selection-state: persist the
  sidebar selected target in workspace-scoped tmux state for later Ctrl-S remote
  endpoint resolution.
last_updated_from:
  file: .agents/tasks/backlog.yaml
  reference: task.remote-create-2-sidebar-selection-state

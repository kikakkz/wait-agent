kind: state
version: 1
current_task: task.event-r2a
focus_statement: task.event-r2a remains the current gate while same-socket main-slot rebinding is validated in a real terminal; the attach-detach switching path is no longer the accepted same-socket implementation, and the next coding pass should audit event-r3 only from the new fixed-chrome rebinding baseline
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: current_task

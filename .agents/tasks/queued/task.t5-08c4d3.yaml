id: task.t5-08c4d3
kind: task
version: 1
title: run end-to-end cross-host validation and acceptance closeout on the corrected runtime-only path
status: ready
priority: high
design_ref:
  doc: docs/remote-network-completion-plan.md
  why:
    - after the export boundary and runtime-only lifecycle semantics are correct, the remaining accepted proof is explicit cross-host product validation on the public `--port` plus `--connect` contract
    - this slice closes phase-2 human-facing acceptance state rather than mixing final validation into lower-level runtime refactors
source_of_truth:
  file: .agents/tasks/backlog.yaml
  reference: next
depends_on:
  - task.t5-08c4d2.done
linked_primitives:
  - primitive.verification-refresh
  - primitive.task-history-update
  - primitive.task-board-sync
  - primitive.agents-self-maintenance
done_criteria:
  - explicit cross-host manual validation passes on the accepted `--port` plus `--connect` path
  - remote open, input, output, resize, disconnect, and reconnect behavior are verified on the corrected session-centric model
  - acceptance docs, status board, and verification state reflect the validated product path
verification:
  - cargo test
  - manual: real cross-host remote interaction validation
touched_paths:
  - src/runtime
  - src/application
  - src/ui
  - docs/execution-status-board.md
  - docs/local-acceptance-checklist.md
  - .agents/tasks/backlog.yaml
  - .agents/tasks/current.yaml
  - .agents/tasks/queued/task.t5-08c4d3.yaml
  - .agents/state/task-history.yaml
  - .agents/state/last-verified.yaml
notes:
  - This slice is acceptance-first. It should not reopen session-model design drift.

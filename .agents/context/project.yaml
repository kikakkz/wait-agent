id: context.project
kind: context
version: 1
summary:
  product: WaitAgent
  repo_type: single_crate_rust_repo
  language: rust
  package_manager: cargo
  architecture_style: tmux_first_local_workspace_runtime
  active_business_goal: resume mirrored network interaction on top of the accepted tmux-first local workspace baseline
  framework_policy: prefer mature maintained Rust components over custom infrastructure, and avoid weakly maintained dependencies across the codebase
current_phase:
  phase_id: phase-2
  phase_title: network aggregation MVP
  stage_id: server-console-activation
  stage_title: build the server-side console on top of the accepted shared catalog and routing boundary
current_focus:
  default_task_id: task.t6-01e
  focus_statement: auto-switch is cancelled; server-console and local workspace attention should stay manual-only and rely on clear waiting-state cues instead of automatic focus jumps
upcoming_focus:
  - keep waiting-state visibility scoped to per-session state, badges, and simple counts rather than queue ordering
  - polish sidebar, picker, badge, and count presentation only if acceptance evidence shows the current cues are too weak
  - do not reintroduce server-side auto-switch without an explicit product reversal
source_of_truth:
  - docs/execution-status-board.md
  - docs/local-acceptance-checklist.md
  - docs/architecture.md
  - docs/tmux-first-workspace-plan.md
  - docs/tmux-first-runtime-architecture.md
  - docs/functional-design.md

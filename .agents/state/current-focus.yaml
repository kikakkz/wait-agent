kind: state
version: 1
current_task: task.ratatui-phase-2-5-session-backend
focus_statement: >
  active task is ratatui migration phase 2.5: migrate local and remote session creation from the
  tmux backend onto the ratatui node server. First slice: add an in-memory session catalog to the
  node server, wire client-to-node commands for creating local and remote sessions, and render the
  catalog in the sidebar using the existing tmux-path row format.
last_updated_from:
  file: .agents/tasks/current.yaml
  reference: task.ratatui-phase-2-5-session-backend.in_progress

id: task.architecture-1
kind: task
version: 1
title: refactor the runtime and module architecture for clearer boundaries and cleaner code
status: deferred
priority: medium
source_of_truth:
  file: .agents/tasks/backlog.yaml
  reference: active
linked_primitives:
  - primitive.task-history-update
  - primitive.task-board-sync
  - primitive.agents-self-maintenance
done_criteria:
  - runtime, display, renderer, and terminal responsibilities are split along stable boundaries rather than accumulating more fullscreen-specific coupling in `src/app.rs`
  - the primary ownership lines for dashboard rendering, fullscreen handoff, lifecycle wrapping, and transcript replay are documented and enforced by module structure
  - superseded glue paths are removed as part of refactor slices instead of being left behind as parallel compatibility branches
verification:
  - targeted architecture review before implementation starts
  - cargo test after each bounded refactor slice
touched_paths:
  - src/app.rs
  - src/lifecycle.rs
  - src/renderer.rs
  - src/terminal.rs
  - src/transcript.rs
  - .agents/tasks/backlog.yaml
  - .agents/tasks/current.yaml
notes:
  - This task is intentionally deferred; recording it here keeps the cleanup debt visible without claiming that implementation has started.
  - Future refactor work must preserve the accepted fullscreen-history baseline and must not reintroduce dashboard-local mouse scrolling as a fallback.
